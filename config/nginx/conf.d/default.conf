# Upstream definitions
upstream frontend {
    server frontend:3000;
    keepalive 64;
}

upstream cms {
    server cms:1337;
    keepalive 64;
}

# HTTP server - redirect to HTTPS in production
server {
    listen 80;
    server_name _;

    # For Let's Encrypt challenges
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Uncomment for production to force HTTPS
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # Development/staging: serve directly
    location / {
        proxy_pass http://frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # CMS API endpoints
    location /api/ {
        limit_req zone=api burst=20 nodelay;
        
        proxy_pass http://cms;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Longer timeouts for API
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    # Strapi admin panel
    location /admin {
        proxy_pass http://cms;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Strapi uploads
    location /uploads {
        proxy_pass http://cms;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # Cache uploaded files
        proxy_cache_valid 200 1d;
        add_header X-Cache-Status $upstream_cache_status;
    }

    # Health checks
    location /_health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}

# HTTPS server (uncomment and configure for production)
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     # SSL certificates
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # Security headers
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # Same location blocks as HTTP server above
#     location / {
#         proxy_pass http://frontend;
#         # ... (same proxy settings)
#     }
#
#     location /api/ {
#         limit_req zone=api burst=20 nodelay;
#         proxy_pass http://cms;
#         # ... (same proxy settings)
#     }
#
#     location /admin {
#         proxy_pass http://cms;
#         # ... (same proxy settings)
#     }
#
#     location /uploads {
#         proxy_pass http://cms;
#         # ... (same proxy settings with caching)
#     }
# }

# China-specific optimizations
# Uncomment if deploying in China
# server {
#     listen 80;
#     server_name cn.your-domain.com;
#
#     # Use domestic CDN for static assets
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         proxy_pass https://cdn.jsdelivr.net;
#         proxy_cache_valid 200 30d;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     # Proxy to frontend for all other requests
#     location / {
#         proxy_pass http://frontend;
#         # ... (same proxy settings)
#     }
# }
