# =============================================================================
# DOCKER COMPOSE ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all environment variables needed for the Docker Compose stack
# Copy this file to .env and fill in the values

# =============================================================================
# NODE ENVIRONMENT
# =============================================================================
NODE_ENV=production

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================
DATABASE_NAME=strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=
DATABASE_PORT=5432
DATABASE_HOST=postgres

# =============================================================================
# STRAPI CMS CONFIGURATION
# =============================================================================
STRAPI_PORT=1337

# Strapi Secret Keys (REQUIRED - Generate secure random strings)
# Generate using: openssl rand -base64 32
APP_KEYS=
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
TRANSFER_TOKEN_SALT=
JWT_SECRET=
ENCRYPTION_KEY=

# =============================================================================
# FRONTEND CONFIGURATION (Nuxt)
# =============================================================================
FRONTEND_PORT=3000

# API URLs (for internal Docker network communication)
NUXT_PUBLIC_API_BASE_URL=http://cms:1337
NUXT_PUBLIC_STRAPI_URL=http://cms:1337

# Strapi API Token (generate from Strapi admin panel)
NUXT_STRAPI_API_TOKEN=

# CDN Configuration (optional, for production)
NUXT_PUBLIC_CDN_URL=

# =============================================================================
# BAIDU API CONFIGURATION (Optional)
# =============================================================================
# Used for Baidu services integration (e.g., translation, search)
BAIDU_API_KEY=
BAIDU_SECRET_KEY=

# =============================================================================
# NGINX REVERSE PROXY (Optional - only if using nginx profile)
# =============================================================================
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# =============================================================================
# CHINA DEPLOYMENT NOTES
# =============================================================================
# For deployments in China:
# 1. Use domestic Docker registry mirrors (see docker-compose.override.example.yml)
# 2. Configure CDN_URL to point to a domestic CDN
# 3. Ensure BAIDU_API_KEY is set if using Baidu services
# 4. Consider using Aliyun, Tencent Cloud, or Huawei Cloud for hosting
