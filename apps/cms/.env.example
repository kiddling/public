# Server Configuration
HOST=0.0.0.0
PORT=1337
NODE_ENV=development

# Security Keys (IMPORTANT: Change these in production!)
APP_KEYS="replace-with-generated-key-1,replace-with-generated-key-2,replace-with-generated-key-3,replace-with-generated-key-4"
API_TOKEN_SALT=replace-with-generated-api-token-salt
ADMIN_JWT_SECRET=replace-with-generated-admin-jwt-secret
TRANSFER_TOKEN_SALT=replace-with-generated-transfer-token-salt
JWT_SECRET=replace-with-generated-jwt-secret
ENCRYPTION_KEY=replace-with-generated-encryption-key

# Database Configuration
# For SQLite (default, good for development)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# For PostgreSQL (recommended for production)
# DATABASE_CLIENT=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=strapi
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strapi
# DATABASE_SSL=false

# For MySQL/MariaDB
# DATABASE_CLIENT=mysql
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_NAME=strapi
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strapi
# DATABASE_SSL=false

# CDN Configuration (for production)
# CDN_URL=https://your-cdn-domain.com

# Chinese Registry Mirror (uncomment for faster downloads in China)
# Set in .npmrc: registry=https://registry.npmmirror.com

# Email Configuration (optional)
# EMAIL_PROVIDER=sendmail
# EMAIL_DEFAULT_FROM=noreply@yourdomain.com
# EMAIL_DEFAULT_REPLY_TO=support@yourdomain.com

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:3000

# Admin Panel Path (optional, defaults to /admin)
# ADMIN_PATH=/admin
